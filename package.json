{
  "name": "taskflow",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.1",
  "scripts": {
    "prepare": "husky install",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "husky": "^9.1.4",
    "pnpm": "9.12.1"
  }
}
